LISTA ROBOTS:

- RECIBE un array de objetos.
- Renderiza una lista con el componente Robot.
- Pasa los datos a Robot.

ROBOTS:

- RECIBE un objeto robot con su información.
- Renderiza una imagen, un h2 con el nombre, un h3 con un titulo, y una lista de caracteríscias.
- Renderiza un boton ELMINIAR.
  - Ejecuta una funcion RECIBIDA al hacer click.
- Renderiza un boton EDITAR.
  - Ejecuta una función RECIBIDA al hacer click.

FORM:

- Renderiza un form con inputs type text.
  - Almacena y envia los datos introducidos en los input.
- Renderica un botón CREAR.
  - Ejecuta una funcion RECIBIDA al hacer click.
